[project]
name = "moodswings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matt Cooper", email = "vtbassmatt@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ms = "moodswings:hello"
moodswings = "moodswings:hello"

[build-system]
requires = ["uv_build>=0.10.10,<0.11.0"]
build-backend = "uv_build"
